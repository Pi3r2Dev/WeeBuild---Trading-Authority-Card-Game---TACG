/**
 * Identité de marque du site (nom actuel : WeeBuild, pas WeBuild).
 * Source unique pour wordmark UI, metadata et copy produit.
 */

/** Wordmark pixel affiché en majuscules (rail, login, bandeau mobile). */
export const SITE_WORDMARK = "WEEBUILD";

/** Nom de marque en prose (FAQ, titres, documentation publique). */
export const SITE_NAME = "WeeBuild";

/** Titre produit complet (onglet navigateur, SEO). */
export const PRODUCT_TITLE = `${SITE_NAME} — Trading Authority Game`;
