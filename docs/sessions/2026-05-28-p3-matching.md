---
date: 2026-05-28
slug: p3-matching
status: open
mode: solo
parent_plan: docs/plans/p3-game-loop-data-model.md
parent_decision: docs/draft-pipeline-ia.md
tags: [p3, matching, embeddings, pgvector, anti-footprint]
---

# Cœur du matching P3 : embeddings à la capture + service de matching (pgvector + rerank + anti-cycle) + persistance des suggestions

## Status
green — tsc --noEmit vert, npm run build vert, tests fonctionnels OK (matching + 3 vérifs anti-cycle), aucune ligne de test laissée en DB.

## Done in this session
- **`lib/services/embeddings.ts`** : `embed(text)` via LiteLLM `gte-qwen2-local` (1536d), troncature/normalisation du texte (`prepareText`, plafond 8k chars), `toPgVector()` (littéral `[a,b,...]` à caster `::vector`). Garde `isConfigured()` (best-effort sans clé).
- **Peuplement à la capture** : `lib/matching/embed-site.ts` (`embedSite(siteId, content)`) branché dans `app/(app)/capturer/actions.ts > persistCapture` après l'upsert Site. Compose titre+description+markdown, écrit via `db.$executeRaw … UPDATE site SET embedding = ${toPgVector}::vector`. **Best-effort** : toute erreur est loggée + avalée, la capture n'échoue jamais.
- **`lib/matching/match.ts`** : `findPartners(siteId, opts)` → `MatchOutcome`. Embedding source lu en DB (sinon embed à la volée + persisté), **pgvector 3× over-fetch** (distance cosine `<=>`), rerank (ou fallback), filtres pertinence (τ) + anti-cycle, top N. Renvoie `PartnerMatch[]` (riche : targetSiteId + scores), pas le `Suggestion` front (lossy).
- **`lib/matching/rerank.ts`** : client `/v1/rerank` LiteLLM ; renvoie `null` si injoignable → fallback ordre cosine. Modèle configurable (`RERANK_MODEL`, défaut `bge_reranker`).
- **`lib/matching/anti-cycle.ts`** : `findForbiddenTargets()` — les 3 vérifs §5 en SQL (`WITH RECURSIVE` pour le cycle court) : (1) arête réciproque, (2) cycle court ≤3, (3) hub trop dense (fenêtre 30j). Seuils dans `DEFAULT_ANTI_CYCLE`.
- **`lib/matching/run.ts`** : `runMatching(siteId)` → crée `MatchingSession` (paramsJson = trace : rerankStatus, over-fetch, exclusions) + `EditorialSuggestion` (createMany, source→cible, `relevanceScore` réel, `articleTopic`/`proposedAnchor` **placeholder** `[À GÉNÉRER]`, status `GENERATED`).
- **`lib/matching/actions.ts`** : server action `triggerMatching(siteId)` protégée par `requireSession()` + garde de propriété (le site doit appartenir au user).
- **`lib/data/index.ts`** : TODO de `getSuggestions`/`getPartners` enrichis (pointent la donnée réelle `EditorialSuggestion`/`findPartners`) mais **restent fixtures** (mapping → type front exige les crédits, hors périmètre ; GAME_LOOP toujours masqué).

## Files touched
- `lib/services/embeddings.ts` — créé : client embeddings gte-qwen2-local + helpers.
- `lib/matching/embed-site.ts` — créé : peuplement Site.embedding (best-effort, raw SQL).
- `lib/matching/match.ts` — créé : `findPartners` (pgvector 3× + rerank/fallback + filtres).
- `lib/matching/rerank.ts` — créé : client rerank avec fallback cosine.
- `lib/matching/anti-cycle.ts` — créé : 3 vérifs anti-cycle (WITH RECURSIVE).
- `lib/matching/run.ts` — créé : `runMatching` (persiste MatchingSession + EditorialSuggestion).
- `lib/matching/actions.ts` — créé : server action `triggerMatching` (requireSession + ownership).
- `app/(app)/capturer/actions.ts` — modifié : appel `embedSite` dans `persistCapture`.
- `lib/data/index.ts` — modifié : commentaires TODO P3 enrichis (comportement inchangé).

## Git state
- Branch: `main` (upstream: none)
- Diverge from main: `+2` / `-0` commits (2 commits locaux non poussés, antérieurs à cette session)
- Uncommitted: 2 modified (`actions.ts`, `index.ts`) + 1 dossier non suivi (`lib/matching/`) + 1 fichier non suivi (`lib/services/embeddings.ts`). `.claude/worktrees/` également non suivi (hors périmètre).
- Last commit: `9898223` docs(p3,p2): handoff fondation + tracker (programme P2/P3 lance)

## Test status
- Snapshot: `green` — `npx tsc --noEmit` exit 0 ; `npm run build` exit 0 (15 routes générées).
- Source: `tsc` + `next build`. Tests fonctionnels ad-hoc (scripts tsx temporaires, supprimés) :
  - embed → dimension 1536 ✅
  - 13 sites seedés embeddés (étaient NULL au départ → maintenant peuplés pour la testabilité)
  - `findPartners(finance-malin.fr)` → 6 candidats classés, source finance match d'abord forbes.fr (rel 0.57) + bourse-debutant.fr (0.51) ✅
  - `runMatching` → MatchingSession + 6 EditorialSuggestion créées, vérifiées en SQL, puis **nettoyées** (0 ligne résiduelle) ✅
  - anti-cycle : insertion de liens synthétiques → les 3 vérifs excluent bien (réciproque/cycle court/hub dense), liens supprimés ✅
- Pas de tests vitest ajoutés (les services existants ont des `.test.ts` ; en ajouter pour le matching = candidat sous-tâche).

## Next concrete step
1. **Génération du texte éditorial** (autre sous-tâche P3) : remplacer les placeholders `[À GÉNÉRER]` par groq-qwen3-32b (FR), sous contrainte anti-footprint §4 (diversité ancres/angles, dédup pgvector via `EditorialSuggestion.embedding`), + `proposedAnchorType`/`naturalScore`.
2. **UI matching + crédits** : brancher `getSuggestions`/`getPartners` sur les `EditorialSuggestion` réelles (mapping → type front, exige l'économie de crédits), puis flip `GAME_LOOP_ENABLED`.
3. Optionnel : tests vitest pour `embeddings.prepareText`/`toPgVector` (purs) et `anti-cycle` (avec fixtures DB).

## Open decisions
- **Contrat de retour `findPartners`** : renvoie `MatchOutcome { matches: PartnerMatch[] }` et non `Suggestion[]` (le type front est lossy, sans targetSiteId). Lean : garder le type riche côté matching, mapper au bord UI.
- **Seuil τ (`minRelevance`)** : défaut 0 (pas de filtre) car calibrage ouvert ; à régler avec les bandes d'autorité (Q metrique §8). Le filtre élément/niveau/langue (§4.3) n'est PAS encore appliqué (seulement cosine + anti-cycle) — à ajouter ?
- **Seuils anti-cycle** (`DEFAULT_ANTI_CYCLE`: depth 2, hub 10/30j) : valeurs de départ prudentes, à calibrer (Q-P3-1).

## Blockers
- **Reranker injoignable en dev** : LiteLLM n'expose AUCUN model alias de rerank pour la clé `sk-webuild` (probe `/v1/models` : pas de `bge_reranker`/`rerank*`), et le conteneur `bge_reranker-…` (GPU :8001) n'a pas de domaine public → fallback ordre cosine actif et documenté dans `rerank.ts`. Le code marchera dès qu'une route `/v1/rerank` sera exposée (modèle via env `RERANK_MODEL`). **Non bloquant** pour le matching (cosine seul donne des résultats cohérents).

## How to resume
1. Lire ce doc + [docs/plans/p3-game-loop-data-model.md](../plans/p3-game-loop-data-model.md) §2/§5 + [docs/draft-pipeline-ia.md](../draft-pipeline-ia.md) §4-5.
2. Regarder `lib/matching/` (match.ts = cœur ; run.ts = persistance ; anti-cycle.ts = §5).
3. Démarrer la sous-tâche « génération texte éditorial » (placeholders `[À GÉNÉRER]` dans run.ts).
