import { PromotionLaunchLoader } from "./PromotionLaunchLoader";

/** Écran B5 — lancer une promotion (visibilité achetée dans le matching). */
export default function PromotionsPage() {
  return <PromotionLaunchLoader />;
}
